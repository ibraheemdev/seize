[package]
name = "seize"
version = "0.1.0"
edition = "2021"

[dependencies]
num_cpus = "1.13.1"
once_cell = "1.9.0"

[target.'cfg(loom)'.dependencies]
loom = { version = "0.5", features = ["checkpoint"] }
